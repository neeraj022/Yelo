# msg = "Hey, we believe our user actions are important for us - hence we've introduced some rewards for you all.
# By the way, are you up on our leaderboard yet?"
# num = Rails.application.secrets.w_mobile_number
# num = User.mobile_number_format(num) 
# sender = User.where(mobile_number: num[:mobile_number]).first
# users = User.allowed
# users.each do |u|
#   User.send_chat_message(sender, u, msg)
# end