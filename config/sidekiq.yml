# Sample configuration file for Sidekiq.
# Options here can still be overridden by cmd line args.
#   sidekiq -C config.yml

:verbose: true
:concurrency:  25
:logfile: /data/rails/log/sidekiq.log
:pidfile: ./tmp/pids/sidekiq.pid
:queues:
  - [notification, 6]
  - [contact_wall, 7]
  - [default, 5]